{
  "name": "my-family",
  "private": true,
  "scripts": {
    "data:build": "node scripts/build-viz-data.mjs",
    "dev": "npm run data:build && next dev",
    "build": "npm run data:build && next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
